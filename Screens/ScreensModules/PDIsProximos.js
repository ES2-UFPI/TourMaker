import React, { Component } from 'react';
import { StyleSheet, View, Text } from 'react-native';
import { Table, TableWrapper, Row, Cell } from 'react-native-table-component';
import {Picker} from '@react-native-picker/picker';

import CustomButton from './ScreensModules/CustomButton';
import CustomMapView from "./ScreensModules/CustomMapView";
import ReactMaps from '../APIs/ReactMaps';

//deve posibilitar a pesquisa de pontos proximos recebendo parametros da localização e se a busca deve retornar mais que um item 

class PDIsProximos extends Component{
    contructor(props){
        //
    }
    render(){
        return(nul)}
}